/* See the deno.jsonc file in the project root for comments. */
{
  fmt: {
    exclude: [
      '**/dist/',
      'deno.jsonc',
    ],
    singleQuote: true,
  },
  lint: {
    exclude: [
      '**/dist/',
    ],
  },
  tasks: {
    build: 'deno run --allow-env --allow-net --allow-read --allow-run=pnpm --allow-write=dist scripts/build.ts',
    check: 'deno lint && deno test --allow-all',
    publish: 'deno run --allow-net --allow-read --allow-run=pnpm --allow-write=dist scripts/publish-npm.ts',
  },
  test: {
    exclude: [
      "**/dist/",
    ],
  },
}
